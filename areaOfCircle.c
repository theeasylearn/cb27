#include<stdio.h>
void main() {
    float radius = 2.2;
    // float PI = 3.14;

    // float area = PI * radius * radius;

    // printf("area = %f", area);

    printf("area = %f", 3.14 * radius * radius);

}